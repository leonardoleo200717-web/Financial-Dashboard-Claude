{
  "name": "financial-dashboard-claude",
  "version": "2.0.0",
  "description": "Personal finance & FIRE planning dashboard (single HTML file). See CLAUDE.md.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node tests/run.js",
    "test:ui": "node build.js && node tests/ui-smoke.js",
    "test:all": "node build.js && node tests/run.js && node tests/ui-smoke.js"
  },
  "devDependencies": {
    "jsdom": "*"
  }
}
